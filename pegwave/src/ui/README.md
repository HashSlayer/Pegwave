# UI

- `App.tsx` — top-level scene router (mainMenu → selectingSection → playing)
- `animations.css` — shared CSS animations (imported via `index.css`)
- `index.ts` — exports `App`

Game views live in `../pegwave/` (MainMenuView, SectionSelectView, GameCanvas).
